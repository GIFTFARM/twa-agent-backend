{
  "name": "twa-agent-backend-root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && npm run start"
  },
  "engines": { "node": "20.x" }
}
