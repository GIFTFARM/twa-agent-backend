{
  "name": "twa-agent-backend-root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo 'no build step'",
    "start": "cd backend && npm install && npm run start"
  },
  "engines": { "node": "20.x" }
}
